<odoo>
    <record id="view_purchase_order_form_inherit_dhl" model="ir.ui.view">
        <field name="name">purchase.order.form.inherit.dhl</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <!-- Botón para solicitar cotización -->
                <button name="request_dhl_quote"
                        string="Solicitar Cotización DHL"
                        type="object"
                        class="btn-primary"/>
                <!-- Botón para generar la orden de DHL -->
                <button name="generate_dhl_order"
                        string="Generar Orden con DHL"
                        type="object"
                        class="btn-success"/>
            </xpath>
            <xpath expr="//field[@name='partner_id']" position="after">
                <field name="customer_id"/>
                <field name="campaign"/>
                <field name="shipping_date"/>
                <field name="stored_selection_options" 
                    invisible="1"
                />
                <field name="shipping_method" 
                    domain="[('purchase_order', '=', id)]"
                />
                <field name="pickup"/>
                <field name="pickup_customer_id"
                    invisible="pickup == False" 
                />
                <field name="weight" 
                    required="shipping_method != False"
                />
                <field name="width" 
                    required="shipping_method != False"
                />
                <field name="length" 
                    required="shipping_method != False"
                />
                <field name="height" 
                    required="shipping_method != False"
                />
                
                <field name='pesoVolumetrico' readonly="1"/>
                <field name='pesoMasa' readonly="1"/>
                <field name='pesoEnvio' readonly="1"/>
                
                <field name="pickup_date" 
                    invisible="pickup == False" 
                />
            </xpath>
            <xpath expr="//field[@name='date_order']" position="before">
                <!-- <field name="campaign"/> -->
                <field name="dhl_status" 
                readonly="1"
                invisible="track_number == False"/>
                <field name="track_number" 
                readonly="1"
                invisible="track_number == False"/>
                <field name="track_url"
                readonly="1"
                invisible="track_url == False"
                widget="url"/>
            </xpath>
        </field>
    </record>
</odoo>
